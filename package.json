{
  "name": "broke-cat-bot",
  "version": "0.8.2",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.mjs",
    "once": "RUN_ONCE=1 node --env-file-if-exists=.env index.mjs",
    "selftest": "node selftest.mjs"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4",
    "bs58": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
